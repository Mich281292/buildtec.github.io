# BuildTec / ICARSA Website

Sitio estático creado localmente. Para ejecutar localmente:

```bash
python -m http.server 8000
# luego abrir http://localhost:8000
```

Contenido: `index.html`, `styles.css`, `script.js` y varias imágenes.
